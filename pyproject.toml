[project]
name = "example-a2a"
version = "0.1.0"
description = "Repository for a sample setup of Agent to agent (A2A) communication"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "a2a-sdk>=1.0.3",
]
